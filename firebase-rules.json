{
  "rules": {
    ".read": false,
    ".write": false,

    "veo3_stats": {
      ".read": false,
      ".write": true
    },

    "veo3_licenses": {
      ".read": false,

      "$code": {
        ".read": true,
        ".write": "data.exists() && newData.exists() && newData.child('plan').val() === data.child('plan').val() && newData.child('durationDays').val() === data.child('durationDays').val() && newData.child('createdAt').val() === data.child('createdAt').val() && newData.child('expiresAt').val() === data.child('expiresAt').val() && newData.child('disabled').val() === data.child('disabled').val()"
      }
    }
  }
}
